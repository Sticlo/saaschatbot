{
  "name": "omitel",
  "private": true,
  "scripts": {
    "build:site": "npm ci --prefix web/site --include=dev && npm run build --prefix web/site",
    "deploy": "npm run build:site && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}

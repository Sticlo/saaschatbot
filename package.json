{
  "name": "omitel",
  "private": true,
  "workspaces": [
    "web/site"
  ],
  "scripts": {
    "build:site": "npm run build --workspace=site",
    "deploy": "npm run build:site && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
